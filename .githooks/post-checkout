#!/usr/bin/env bash
# Pull memory after branch checkout (only on full checkout, not file checkout).
# $3 is "1" for a branch checkout, "0" for a file checkout.
if [ "$3" = "1" ]; then
  node "$(git rev-parse --show-toplevel)/.claude/scripts/sync-memory.mjs" pull || true
fi
